{
  "name": "ec2-proxy",
  "version": "1.0.0",
  "description": "Network code for spinService",
  "main": "index.js",
  "scripts": {
    "build": "esbuild index.ts --bundle --minify --sourcemap --platform=node --target=es2020 --outfile=dist/index.js",
    "start": "node dist/index.js"
  },
  "author": "Jabhi",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^5.0.3",
    "express": "^5.1.0"
  },
  "dependencies": {
    "@aws-sdk/client-cloudwatch": "^3.826.0"
  }
}
